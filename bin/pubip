#!/usr/bin/env bash
# pubip - print your public IP address
set -euo pipefail

if command -v curl &>/dev/null; then
  curl -sf https://ifconfig.me
elif command -v wget &>/dev/null; then
  wget -qO- https://ifconfig.me
else
  echo "pubip: needs curl or wget" >&2
  exit 1
fi
echo
